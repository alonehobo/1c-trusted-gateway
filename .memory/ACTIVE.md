# Активная задача

## Статус
Нет активного изменения

## Текущая задача
Шаблонный проект без поднятого `Change ID`. Для нового проекта сначала выполни `/prj-init`.

## Блокеры
нет

## Next action
Если проект только что скопирован из шаблона, запустить `/prj-init`. Иначе при следующей существенной задаче создать новый `Change ID` и заполнить `ACTIVE.md` заново.
