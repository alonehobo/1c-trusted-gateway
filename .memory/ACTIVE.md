# Активная задача

## Статус
Задачи 1 и 3 реализованы. Все unit-тесты зелёные, бинарь собран. Остался manual smoke на реальной базе.

## Change ID
2026-04-14-type-aware-masking

## Текущая задача
Type-aware маскирование: классификация колонок по типу метаданных 1С вместо whitelist'а по именам полей.

- **Задача 1** (strip `ПРЕДСТАВЛЕНИЕ()`) — **готово**: `stripPresentationCalls` в `query_normalize.go`, подключено в MCP- и UI-путях, 16 тестов.
- **Задача 3** (type-aware маскирование) — **готово**: `type_policy.go` + дефолты + persistence; клиент шлёт `IncludeSchema: true`; парсер `extractRowsWithSchema`; sanitizer интегрирован; UI-раздел «Политика типов».
- **Задача 2** (1С MCP `IncludeSchema`) — выполнена ранее в отдельном репозитории.

## Блокеры
нет

## Next action
1. Manual smoke: запрос через UI и через MCP-агента на реальной базе — убедиться, что колонки-перечисления/счета/числа не запрашивают whitelist, а справочники маскируются.
2. Проверить, что UI редактора политики типов сохраняется в `settings.bin` и переживает перезапуск.
3. Возможные будущие мелкие улучшения (не блокирующие):
   - Бейдж `truncated: N` в шапке таблицы результатов.
   - Тултип с перечислением типов колонки.

## Параллельные/приостановленные
- `2026-04-13-ux-improvements`: UX-улучшения P1-P3 — в работе, отдельными сессиями.
